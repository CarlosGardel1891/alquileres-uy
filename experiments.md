# Experiments — alquileres-uy

## Fixture contract runs

**These numbers come from synthetic fixture data and are not project or
market results.** They exist so we can prove the pipeline runs
end-to-end and produces stable artifacts.

Fixture: `tests/fixtures/models/etl_run` — 240 rows, 183 unique dates,
generated by `scripts/_generate_model_fixture.py` with seed `20260805`.

Row counts: train `169`, validation `35`, test `36`. Split cutoffs:
train → validation `2025-07-30`, validation → test `2025-09-13`.

| Run    | Data mode | Model    | Features                                                                | Hyperparameters                        | Validation MAE (USD) | Test MAE (USD) | MAPE (val) | Notes |
| ------ | --------- | -------- | ----------------------------------------------------------------------- | -------------------------------------- | -------------------- | -------------- | ---------- | ----- |
| fixture| fixture   | baseline | median price/m² by (neighborhood, property_type) with fallback chain     | n/a                                    | 55.32                | 154.97         | 5.6%       | reference floor |
| fixture| fixture   | linear   | numeric (impute+scale) + one-hot categorical                            | Ridge alpha=1.0                        | 51.74                | 151.48         | 5.2%       | selected by validation MAE |
| fixture| fixture   | lightgbm | numeric + one-hot categorical                                            | lr=0.05, num_leaves=15, min_child=5    | 38.78                | 151.38         | 3.9%       | best classical on validation |
| fixture| fixture   | torch    | numeric (mean/std) + 2 embeddings + MLP [64,32], dropout 0.1             | SmoothL1, Adam lr=1e-3, patience=15    | 70.34                | 206.56         | 7.1%       | trained for contract; not deployable |

**Fixture selection result:**

* `best_overall_model = lightgbm`
* `serving_candidate = lightgbm` (torch excluded by architecture decision)
* `deployable = false`, `blocked_reason = "fixture training run"`

## Production results — pending ETL_PRODUCTION_VALIDATED

_No real training run has been executed._ This section will be filled
in only after:

1. an ETL run finishes with `data_mode == "real"`;
2. a signed `etl_production_approval.json` is issued by the TL;
3. the training pipeline consumes both and publishes an artifact
   bundle whose metadata carries `deployable=true`.

Until then, no numbers are entered here. Nothing about the Montevideo
rental market can be inferred from the fixture rows above.
